'use client';
import {FormEvent,useState} from 'react'; import {useRouter} from 'next/navigation'; import {supabase} from '@/lib/supabase';
export default function Login(){const r=useRouter();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [mode,setMode]=useState<'login'|'signup'>('login');const [msg,setMsg]=useState('');const [busy,setBusy]=useState(false);
async function submit(e:FormEvent){e.preventDefault();setBusy(true);setMsg('');const s=supabase();const res=mode==='login'?await s.auth.signInWithPassword({email,password}):await s.auth.signUp({email,password});if(res.error)setMsg(res.error.message);else if(mode==='signup')setMsg('Account created. If email confirmation is enabled in Supabase, confirm your email, then log in.');else r.replace('/dashboard');setBusy(false)}
return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:20}}><div className="card" style={{width:'100%',maxWidth:420,padding:30}}><div style={{fontWeight:800,fontSize:24}}>Impact Water <span style={{color:'#2563eb'}}>OS</span></div><p className="muted" style={{marginTop:6}}>E-commerce & Growth Operating System</p><form onSubmit={submit} style={{display:'grid',gap:14,marginTop:24}}><label>Email<input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)} /></label><label>Password<input className="input" type="password" required minLength={6} value={password} onChange={e=>setPassword(e.target.value)} /></label>{msg&&<div style={{fontSize:13,color:'#374151',background:'#f3f4f6',padding:10,borderRadius:8}}>{msg}</div>}<button className="btn btn-primary" disabled={busy}>{busy?'Please wait…':mode==='login'?'Log in':'Create account'}</button></form><button className="btn" style={{marginTop:12,width:'100%'}} onClick={()=>setMode(mode==='login'?'signup':'login')}>{mode==='login'?'Create a new account':'Back to login'}</button></div></main>}
